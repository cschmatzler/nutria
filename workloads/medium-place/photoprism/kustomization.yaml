resources:
  - configuration/photoprism-env.yaml.enc
  - statefulset.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: photoprism-env
    namespace: medium-place
    env: configuration/photoprism.env
