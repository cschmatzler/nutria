apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint

metadata:
  name: library.medium.place
  namespace: medium-place

spec:
  endpoints:
    - dnsName: library.medium.place
      recordType: CNAME
      targets:
        - nutria.cschmatzler.github.beta.tailscale.net
