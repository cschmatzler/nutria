apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint

metadata:
  name: treasury.medium.place
  namespace: medium-place

spec:
  endpoints:
    - dnsName: treasury.medium.place
      recordType: CNAME
      targets:
        - nutria.cschmatzler.github.beta.tailscale.net
